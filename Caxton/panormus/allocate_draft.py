import pandas as pd

def getAssetsList(basket_name):
    # func to collect the list of assets which have been used across all baskets
    return currency_all,currency_strat

def aggResults(basket_name):
    # sub to aggregate the currency allocation across basket
    # collect the risk budget and scaling parameters for the basket
    
